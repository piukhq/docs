server {
    listen      80;
    server_name localhost;

    location / {
        root    /usr/share/nginx/html;
        index   ${DEPLOYED_ENV}.html;
    }

    location /bpl {
        root    /usr/share/nginx/html;
        index   /bpl/index.html;
    }

    location /healthz {
        return  204;
    }
}
